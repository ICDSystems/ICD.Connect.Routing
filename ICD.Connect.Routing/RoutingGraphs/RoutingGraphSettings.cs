using ICD.Connect.Settings.Attributes;

namespace ICD.Connect.Routing.RoutingGraphs
{
	[KrangSettings("RoutingGraph", typeof(RoutingGraph))]
	public sealed class RoutingGraphSettings : AbstractRoutingGraphSettings
	{
	}
}
